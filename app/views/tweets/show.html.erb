<h1>詳細</h1>

<h3><%= @tweet.body %></h3>
<p><%= @tweet.updated_at %><p>
<%= link_to  "戻る", :back %>

<%= link_to "変更",edit_tweet_path(@tweet)%>
<%= link_to '消しますよ', @tweet, method: :delete, data:{ confirm: 'Are you sure?' } %>